# init.recovery.lynx.rc — Pixel 7a (lynx) stub.
# Loaded by init when ro.hardware=lynx. All actual init code is in init.recovery.pixel_common.rc.

on early-init
    setprop DOF_SCREEN_H 2400

import /init.recovery.pixel_common.rc
